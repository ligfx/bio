<!DOCTYPE html>
<html lang="en">

<head>
	<style type="text/css">
		{{ render("results.css") }}
	</style>
	
	<script type="text/javascript" src="raphael-min.js"></script>
	<script type="text/javascript">
		{{ render("app.js.jinja2") }}
	</script>
	<script type="text/javascript">
	
		(function() {
	
			var alignments = [
				{% for organism, data in data.by_organism() %}
					{% for alignment, data in data.by_alignment() %}
						"{{ alignment.digest }}",
					{% endfor %}
				{% endfor %}
			];
	
			var scrolling = (function() {
				var lastScroll = [0, 0];
				return {
					save: function() {
						var hscroll = (document.all ? document.scrollLeft : window.pageXOffset);
						var vscroll = (document.all ? document.scrollTop : window.pageYOffset);
						lastScroll = [hscroll, vscroll];
					},
					restore: function() {
						window.scrollTo(lastScroll[0], lastScroll[1]);
					}
				};
			})();
	
			domReady(function() {
				$.each(alignments, function(d) {
					$("alignment-" + d).click(function() {
						scrolling.save();
						
						$('overlays').show();
						$('alignment-overlay-' + d).show();
						$('baseLayer').hide();
					});
					$("alignment-overlay-close-" + d).click(function() {
						$('overlays').hide();
						$('alignment-overlay-' + d).hide();
						$('baseLayer').show();
						
						scrolling.restore();
					});
				});
			});
			
		}());
	</script>
</head>

<body>

<div id="overlays">
	{% for organism, data in data.by_organism() %}
	{% for alignment, data in data.by_alignment() %}
		<div id="alignment-overlay-{{ alignment.digest }}">
			<div class="actions">
				<a id="alignment-overlay-close-{{ alignment.digest }}"
				   href="#close">Close popup</a>
			</div>
			
			<h1>{{ organism }}</h1>
			<h1>{{ alignment.name }}</h1>
			
			{% for hsp, datum in data.by_hsp() %}
					<div class="hsp" style="border: 1px solid #333; padding: 5px">
						<h1>Query sequence: {{ datum.record.query }}</h1>
						<h2>E-value: {{ hsp.evalue }}</h2>
						<h2>Score: {{ hsp.score }}</h2>
						<pre>Sbjct: {{ hsp.subject }}<br/>Match: {{ hsp.match }}<br/>Query: {{ hsp.query }}</pre>
						<p></p>
						<p>Query {{ datum.record.width }} nt: {{ hsp.query_start }} &ndash; {{ hsp.query_end }}</p>
						<div class="hsp_border {{ hsp.strength }}" style="
							width: {{ hsp.query_width | as_percent(datum.record.width) }};
							height: {{ hsp.evalue }}
							margin-left: {{ hsp.query_start | as_percent(datum.record.width) }};">
						</div>
						<div style="height: 5px; background-color: black"></div>
						
						<p>Subject {# alignment.length #} nt: {{ hsp.subject_start }} &ndash; {{ hsp.subject_end }}</p>
						<div class="hsp_border {{ hsp.strength }}" style="
							width: {# hsp.subject_width | as_percent(alignment.length) #};
							margin-left: {# hsp.subject_start | as_percent(alignment.length) #}">
						</div>
						<div style="height: 5px; background-color: black"></div>
						
						<div id="paper-{{alignment.digest}}-{{ datum.record.query }}"></div>
					</div>
			{% endfor %}
		</div>
	{% endfor %}
	{% endfor %}
	<div style="clear: both"></div>
</div>

<div id="baseLayer">

	<figure id="colorkey">
		<figcaption>Color key for alignment scores</figcaption>
		<div class="poor">&lt; 40</div>
		<div class="fair">40 &ndash; 50</div>
		<div class="okay">50 &ndash; 80</div>
		<div class="good">80 &ndash; 200</div>
		<div class="great">200 &le;</div>
	</figure>

	<div style="clear: both"></div>

	<div class="organisms">

	{% for organism, data in data.by_organism() %}
	<section class="organism">
		<header>
			<div class="organismName">
				{{ organism }}
			</div>
			<div class="sequences">
				{% for seq in sequences %}
					<div class="sequence" style="
						width: {{ seq | length | as_percent(input_width) }}">
						<div class="sequenceName">{{ seq.name }}</div>
						<div class="sequenceLength">{{ seq | length }} nt</div>
					</div>
				{% endfor %}
				<div style="clear: both"></div>
			</div>
		</header>

			<div class="alignmentContainer">
			{% for alignment, data in data.by_alignment() %}
				<a id="alignment-{{ alignment.digest }}"
				   href="#alignment-{{ alignment.digest }}">
				<div class="alignment">
			
					<div class="divider">

						{% for seq in sequences[0:-1] %}
							<div class="divider" style="
								left: {{ seq | length | as_percent(input_width) }}"
							>
						{% endfor %}
						
						<div class="alignmentGraphics" style="
							right: {{ sequences[:-1] | map_function('len') | sum | as_percent(input_width) }}"
						>
							<div class="alignmentName">
								<strong>{{ alignment.name }}</strong><br/>
								<em>{{ data.lowest_evalue() }}</em>
							</div>
						
							{% for record, data in data.by_record() %}
								<div class="graphic" style="
									width: {{ record.width | as_percent(input_width) }};
									margin-left: {{ record.offset | as_percent(input_width) }}"
								>
									{% for datum in data %}
									{% with hsp = datum.hsp %}
										<div class="hsp_border {{ hsp.strength }}" style="
											margin-left: {{ hsp.query_start | as_percent(record.width) }};
											width: {{ hsp.query_width | as_percent(record.width) }}">
										</div>
									{% endwith %}
									{% endfor %}
								</div>
							{% endfor %}
							
							{% for seq in sequences[0:-1] %}
								</div>
							{% endfor %}
						</div>
						
					</div>
				
					<div style="clear: both"></div>
				</div>
				</a>
			{% endfor %}
		</div>

	</section>
	{% endfor %}

	</div>

</div><!-- #baseLayer -->

<script type="text/javascript">
	var paper = Raphael("paper-102620776a20dc866709038f2ade720e-test", 120, 200);
	
	paper.path("M71 0L120 0").attr({
		"stroke": "red",
		"stroke-width": 5
	});
	paper.path("M0 151L0 199").attr({
		"stroke": "red",
		"stroke-width": 5
	});
	paper.path("M71 0L71 200").attr({
		"stroke": "#ddd",
		"stroke-width": 2,
		"stroke-dasharray": "-"
	});
	paper.path("M0 151L120 151").attr({
		"stroke": "#ddd",
		"stroke-width": 2,
		"stroke-dasharray": "-"
	});
	paper.path("M71 151L120 199").attr({
		"stroke": "red",
		"stroke-width": 8
	});
	
	
</script>

</body>

</html> 
